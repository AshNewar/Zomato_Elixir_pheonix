{

  "scripts": {
    "deploy": "npm run build",
    "build": "esbuild js/app.js --bundle --minify --sourcemap --target=es2017 --outdir=../priv/static/js --external:phoenix_html --external:phoenix --external:phoenix_live_view"
    },

  "dependencies": {
    
    "chart.js": "^4.4.5"


  },

  "devDependencies": {

    "esbuild": "^0.24.0"

  }

}

